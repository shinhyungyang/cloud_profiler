#include "net_conf.h"

bool getConfig(std::string ch, handler_type * h, uint64_t * par)  {
  return false;
}

bool getConfig(std::string ch,
               std::string dotted_quad,
	       uint32_t port,
               uint32_t pid,
               uint32_t tid,
               int32_t  ch_nr,
               handler_type * h, uint64_t * par) {
  return false;
}
